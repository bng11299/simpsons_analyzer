## To run analysis:

1. Add CSV files to directory, as these are not included in the repository
2. `python3 -m venv venv`
3. `venv/bin/python3 -m pip install -r requirements.txt`
4. `venv/bin/python3 simpsons_analyzer.py`
5. Open generated .html file in a web browser